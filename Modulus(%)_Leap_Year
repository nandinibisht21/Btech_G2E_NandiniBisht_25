#include <stdio.h>
int main(){
    int a ;
    printf("Enter a : ");
    scanf("%d", &a  );
    
    if (a %4 ==0 ){
        printf(" The Year is a Leap Year");
    }
    else
        {
            printf(" The Year is not a leap year");
        }
}
