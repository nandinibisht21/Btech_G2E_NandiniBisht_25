#include <stdio.h>
int main(){
    int a ;
    printf("Enter a : ");
    scanf("%d", &a  );
    
    if (a %2 ==0 ){
        printf(" The number a is even");
    }
    else
        {
            printf(" The number a is odd");
        }
}
